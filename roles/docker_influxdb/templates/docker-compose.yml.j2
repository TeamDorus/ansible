---
version: "3"

services:

  influxdb:
    container_name: influxdb
    image: influxdb:2.4.0
    restart: unless-stopped
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    ports:
      - 8086:8086
    volumes:
      - "{{ influxdb_volume_dir }}:/var/lib/influxdb2"
